[org 0x7c00]
    mov bx, WELCOME
    call print_string
    mov bx, GOODBYE
    call print_string

    jmp $

    %include "print_string.asm"

    WELCOME:
        db 'Welcome user!', 0

    GOODBYE:
        db 'Goodbye user!', 0

    times 510 - ($ - $$) db 0
    dw 0xaa55
